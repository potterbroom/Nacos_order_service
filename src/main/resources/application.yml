spring:
  application:
    name: nailong-order

  datasource:
    url: jdbc:mysql://localhost:3306/nailong?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 9001

mybatis-plus:
  mapper-locations: classpath:/mappers/*.xml
  type-aliases-package: org.csu.nailong.order.entity